# Blockchain-Based Chain of Custody for Digital Forensics

**FYP — M. Talha | Roll No: fa-2022/BS/DFCS/075**

---

## Project Overview

A tamper-resistant digital evidence workflow that combines:

- **Ethereum (Ganache)** — immutable on-chain evidence (same as before)
- **Solidity** — smart contract (`contracts/ChainOfCustody.sol`)
- **Python + Flask** — web backend (blueprints for API, help, features, admin)
- **SQLAlchemy + SQLite** — relational data (assignments, audits, alerts, help topics, etc.)
- **Web3.py** — contract calls and transactions
- **SHA-256** — integrity hashing
- **cryptography (Fernet)** — optional at-rest encryption for uploads
- **Bootstrap 5** — responsive UI

---

## Roles and permissions

| Role | Capabilities |
|------|----------------|
| **Admin** | Full control: manage users and cases, assign evidence to investigators/members, approve access requests, view/edit/delete any data, set time locks |
| **Investigator** | Access **only cases assigned by Admin**; upload evidence, analyze case data, generate visualizations; cannot delete/modify system config or unauthorized cases |
| **Member** | **Basic user**: request access to a case (pending admin approval), view **only their own assigned cases**; no access to other users’ data |

Investigators and Members get cases only through **case assignments** (Admin assigns evidence IDs to them). Members can also submit **case access requests** that Admins approve.

---

## Where data is stored

### 1. **Ganache / blockchain (unchanged model)**

- **Evidence records** (`addEvidence` on contract): hash, filename, type, size, uploader — **on-chain** via the smart contract.
- **Custody events** (`logCustodyEvent`): action, actor, notes, timestamps — **on-chain**.
- **Verification** (`verifyEvidence`): hash comparison — **on-chain** (read-only).

So the **on-chain part is still the same**: one contract, one chain, same Solidity logic. Nothing here moved to JSON files.

### 2. **SQLite database**

Everything relational is in **SQLite** (SQLAlchemy), not JSON files:

| Data | Table |
|------|--------|
| Help topics | `help_topics` |
| Forensic cases & links | `forensic_cases`, `case_evidence_links` |
| Off-chain meta (local path, optional IPFS CID) | `offchain_evidence_meta` |
| Transfer requests | `transfer_requests` |
| Audit log (geo, HMAC signature) | `audit_log_entries` |
| Security alerts | `security_alerts` |
| Verification history | `verification_records` |
| Time locks | `evidence_time_locks` |
| **Case assignments** (Admin → Investigator/Member) | `case_assignments` |
| **Case access requests** (Member → pending/approved) | `case_access_requests` |

**Summary:** **Blockchain evidence stays on Ganache**; **everything else is in SQLite** (not JSON). The app uses **both**: chain + database — chain for immutability, DB for roles, assignments, audits, graphs, etc.

---

## Project Structure

```
fyp/
├── contracts/
│   ├── ChainOfCustody.sol            ← Solidity smart contract
│   ├── contract_abi.json             ← Generated by deploy.py (or Remix) — Web3 ABI
│   ├── contract_address.txt          ← Generated by deploy.py — one line 0x address
│   └── contract_address.example.txt  ← Template; copy to contract_address.txt
├── backend/
│   ├── app.py                        ← Flask application entrypoint
│   ├── auth.py                       ← login_required, role_required, admin_required, RBAC helpers
│   ├── blockchain_utils.py         ← Web3, contract, chain helpers (reads ../contracts/*)
│   ├── database.py                   ← SQLAlchemy models & SQLite (custody.db)
│   ├── api_routes.py               ← JSON API blueprint (/api/...)
│   ├── help_routes.py              ← Help pages blueprint (/help/...)
│   ├── features_routes.py           ← Cases, transfers, reports, time locks
│   ├── admin_routes.py             ← Admin-only: users, case assignment, access requests
│   ├── custody.db                  ← SQLite DB (created on first run)
│   └── fernet.key                  ← Generated on first run (encryption key)
├── frontend/
│   ├── templates/
│   │   ├── base.html
│   │   ├── login.html
│   │   ├── dashboard.html
│   │   ├── upload.html
│   │   ├── evidence_list.html
│   │   ├── evidence_detail.html
│   │   ├── verify.html
│   │   ├── admin_home.html, admin_users.html, admin_assign.html, admin_requests.html
│   │   └── help/
│   │       ├── index.html
│   │       └── topic.html
│   └── static/
│       ├── css/style.css
│       └── js/main.js
├── uploads/                         ← Stored evidence files (encrypted or plain)
├── deploy.py                        ← Compile & deploy contract (run once)
├── requirements.txt
├── .gitignore
└── README.md
```

---

## Prerequisites

- **Python 3.10+** recommended
- **Ganache** (or compatible JSON-RPC node) on `http://127.0.0.1:7545`
- **Git** (optional), for cloning the repository

---

## Setup Instructions

### 1. Install Ganache

Download **Ganache** from [Truffle Suite](https://trufflesuite.com/ganache/), start a workspace (e.g. Quickstart), and confirm it listens on **port 7545** (default for Ganache GUI).

### 2. Python environment

From the project root (`fyp/`):

```bash
python3 -m venv .venv
source .venv/bin/activate          # Linux / macOS
# .venv\Scripts\activate           # Windows

python3 -m pip install --upgrade pip
pip install -r requirements.txt
```

### 3. Deploy the smart contract

From the project root:

```bash
python deploy.py
```

This installs the Solidity compiler (**solc 0.8.7**, as configured in `deploy.py`), compiles the contract, deploys it to Ganache, and writes:

- `contracts/contract_abi.json`
- `contracts/contract_address.txt`

### 4. Run the Flask app

```bash
cd backend
python app.py
```

Open **http://127.0.0.1:5000** in your browser.

---

## Demo login credentials

| Username       | Password     | Role          |
|----------------|--------------|---------------|
| `admin`        | `admin123`   | Admin         |
| `investigator` | `inv123`     | Investigator  |
| `analyst`      | `analyst123` | Analyst       |
| `member`       | `member123`  | Member (basic user) |

---

## How it works

### Evidence upload

1. User uploads a file (allowed types are enforced in `app.py`).
2. The server computes **SHA-256** of the original bytes.
3. Optionally the file is **encrypted** with Fernet and saved under `uploads/`.
4. Hash and metadata are sent to the contract via **`addEvidence`**.
5. The transaction is mined on Ganache; the record is append-only on-chain.

### Verification

1. User supplies an evidence ID and a file.
2. A new hash is computed and checked with **`verifyEvidence`**.
3. A **Verified** custody event can be logged on-chain.

### Custody chain

Actions (e.g. transfer, analyze, verify) are recorded with **`logCustodyEvent`**, producing an immutable timeline per evidence ID.

### Help pages

After login, **Help** in the navbar links to topic pages. Copy is stored in **SQLite** (`help_topics` table) and loaded through **SQLAlchemy**; the database is initialized when the app starts.

### Role-based access

- **Admin** sees all evidence and cases; can assign evidence to investigators/members; approves case access requests.
- **Investigator** sees only evidence IDs listed in **`case_assignments`** for their username.
- **Member** sees only evidence IDs assigned to them (same table); can submit **case access requests** for approval.

### Admin panel

**Admin** users can open **Administration** in the navbar to:

- Manage users and roles (demo: edit `users_config.py` or use the admin users form)
- Assign blockchain evidence IDs to investigators or members
- Approve or reject **case access requests** from Members

---

## API (JSON)

| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| `GET` | `/api/status` | No | Ganache connection and contract deployment flags |
| `GET` | `/api/evidence` | Yes (session) | List evidence summaries |
| `GET` | `/api/evidence/<id>/chain` | Yes (session) | Custody events for one evidence ID |

Use a logged-in browser session or mirror the same cookie when calling protected routes.

---

## Smart contract (summary)

| Function | Role |
|----------|------|
| `addEvidence(...)` | Register new evidence metadata and hash |
| `logCustodyEvent(...)` | Append a custody event |
| `verifyEvidence(id, newHash)` | Compare stored vs new hash |
| `getCustodyChain(id)` | Full event list for an item |
| `getEvidence(id)` | Metadata for one item |
| `getEvidenceCount()` | Total count |

---

## Technologies

| Technology | Purpose |
|------------|---------|
| Ganache | Local Ethereum chain |
| Solidity 0.8.x (deploy uses 0.8.7) | Smart contract |
| Python 3.10+ | Backend |
| Flask 3.x | Web framework |
| SQLAlchemy 2.x | ORM for SQLite |
| Web3.py | Ethereum JSON-RPC / contract ABI |
| py-solc-x | Solidity compiler integration |
| cryptography (Fernet) | Symmetric encryption |
| Bootstrap 5 | UI |

---

## Repository hygiene

- **`.gitignore`** excludes virtualenvs, `__pycache__`, `.env`, build artifacts, and common editor caches.
- Do **not** commit secrets: treat `backend/fernet.key`, local `.env`, and production databases with care.

---

## Future scope

- Cloud or container deployment
- Stronger authentication (e.g. database-backed users)
- Mobile or API clients using the same JSON endpoints
- Integration with forensic tooling
- Decentralized storage (e.g. IPFS) for large artifacts
